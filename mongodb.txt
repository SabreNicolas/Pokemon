Si mongodb ne fonctionne pas :

supprimer /data/db puis : (pas ouf car si on stocke les données dedans, on va tout perdre mais ça peut dépanner ! en plus données stockées en format JSON)

sudo mkdir -p /data/db
sudo chown `id -u` /data/db
mongod

show dbs => lister bdd

http://127.0.0.1:27017 => accès via web mais ne marche pas

chercher le chemin du répertoire ou sont stockées les bdd

installation de compass : sudo gem install compass